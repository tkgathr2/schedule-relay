{
  "name": "schedule-relay",
  "version": "0.1.0",
  "private": true,
  "description": "スケジュール調整ツール（Spir全機能＋リレー型T6）— 決定論的スケジューリングコア",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
